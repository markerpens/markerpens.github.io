document.addEventListener("DOMContentLoaded", () => {
    // Directly show the content with fade-in and slide effect
    setTimeout(() => {
        document.querySelector('.content').classList.add('show');
    }, 500);  // 0.5 seconds, adjust for delay if needed
});
